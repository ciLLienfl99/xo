# OxideTerm — custom Mac ARM64 bundle

Personal build of OxideTerm for Apple Silicon (aarch64-apple-darwin).

Requested changes:
- Active-session actions in a right-click context menu.
- Independent active-session font size setting.
- macOS bundle-local portable data with a data-preserving update path.

Source baseline: AnalyseDeCircuit/oxideterm, commit `4d5933c9914ab5c5efc4ff4124bdf255f6ba619d` (the source archive supplied by the repository owner).

Build setup is being prepared. No compiled application is available yet.

This public repository must contain source/build files only, never connection profiles, credentials or UserData.

Upstream licensing and notices will be retained with the build source.
